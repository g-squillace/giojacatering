fragment slideshowRecord on SlideshowRecord {
  id
  _modelApiKey
  slides {
    id
    title
    year
    text
    image {
      responsiveImage(
        imgixParams: { auto: format, ar: "4:3", fit: crop, h: 720 }
      ) {
        ...rImage
      }
    }
  }
}
